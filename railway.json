{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c \"import os; os.chdir('backend') if os.path.isdir('backend') else None\" && gunicorn clip_server:app --bind 0.0.0.0:${PORT:-8080} --workers 1 --threads 4 --timeout 120",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
